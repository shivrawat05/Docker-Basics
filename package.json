{
  "name": "crud",
  "version": "1.0.0",
  "description": "This test app for docker",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "dev-docker": "copy .env.docker .env && docker-compose -f docker-compose.yml up -d  --build",
    "dev-local": "copy .env.local .env && docker-compose -f docker-compose.yml up -d --build app redis"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "pg": "^8.16.3",
    "redis": "^5.8.2"
  }
}
